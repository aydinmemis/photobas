import React from 'react';
import { View, Touchable, Text, StyleSheet, Image } from 'react-native';

const ImageSelect = () => (
  <View>
    <View>
      <Image />
    </View>
  </View>
);

export default ImageSelect;
